# Environment variables
.env
*.env
.env.*
env.bak/
venv.bak/

# Python cache files and artifacts
__pycache__/
*.py[cod]
# *$py.class  # Consider if this is truly needed; .pyc/.pyo are covered by *.py[cod]
*.egg-info/
dist/
build/
*.so
*.coverage
.tox/
.nox/
.hypothesis/
celerybeat-schedule
*.pid
pip-log.txt
pip-delete-this-directory.txt

# Django specific
django_cache_data/ # For Django's file-based cache

# Virtual environment
.venv/
venv/
env/
ENV/

# Django collected static files
staticfiles_collected/

# Media files (user-uploaded content)
media/

# Log files
*.log
logs/
# The following are quite specific, *.log and logs/ often suffice, but they don't hurt.
# *.log.*
# *.log.[0-9]
# *.log.[0-9][0][0-9]

# IDE files
.vscode/
.idea/
*.project
*.swp
*~
*.sublime-project
*.sublime-workspace
nbproject/

# OS generated files
.DS_Store
Thumbs.db
Desktop.ini
._*

# Database files (if using local SQLite and don't want to commit)
# db.sqlite3
# *.sqlite
# *.sqlite3

# Node.js / Tailwind CSS specific
node_modules/
npm-debug.log*
yarn-error.log*
yarn-debug.log*

# Generated Tailwind CSS output file (if you always build it and don't commit the output)
# static/css/output.css # Your call: commit if it's the source of truth for deployment without a build step there, ignore if always built.

# Sensitive Files (SHOULD NOT BE COMMITTED TO GIT)
audiox-auth-e32bbe6bfa04.json

# Other common ignores
.python-version
.pylintrc
coverage.xml
nosetests.xml

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# Sass cache
.sass-cache/

# Python session history (e.g., from IPython)
.python_history

# Build artifacts from various tools
/out/
/target/