# Environment variables
.env
*.env
.env.*
env.bak/
venv.bak/

# Python cache files and artifacts
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.so
*.coverage
.tox/
.nox/
.hypothesis/
celerybeat-schedule
*.pid
pip-log.txt
pip-delete-this-directory.txt

# Virtual environment
.venv/
venv/
env/
ENV/

# Django collected static files and media
# staticfiles_collected/ is typically generated by collectstatic for deployments
staticfiles_collected/

# media/ directory:
# If your media/ directory contains user-uploaded files that are specific to an environment
# or backed up elsewhere, ignoring it is fine.
# If it contains essential media assets for the site to function (e.g., default images),
# you might want to commit those specific assets and ignore only user-generated subfolders within media/.
media/

# Log files
*.log
logs/
*.log.*
*.log.[0-9]
*.log.[0-9][0-9]

# IDE files
.vscode/
.idea/
*.project
*.swp
*~
*.sublime-project
*.sublime-workspace
nbproject/

# OS generated files
.DS_Store
Thumbs.db
Desktop.ini
._*

# Database files (if using SQLite for local dev and don't want to commit)
# db.sqlite3
# *.sqlite
# *.sqlite3

# Node.js / Tailwind CSS specific
node_modules/
npm-debug.log*
yarn-error.log*
yarn-debug.log*
package-lock.json # If you prefer to only commit package.json and let collaborators generate their own lock file (less common for consistency)
# However, it's generally recommended to commit package-lock.json to ensure consistent installs.
# If you commit package-lock.json, remove the line above.

# Generated Tailwind CSS output file
# Path is AUDIOX - WEBAPP/AudioX/static/css/output.css
# Relative to this .gitignore file (which is in AUDIOX - WEBAPP/AudioX/):
static/css/output.css

# Other common ignores
.python-version
.pylintrc
coverage.xml
nosetests.xml
# pip-freeze.txt # Usually requirements.txt is the source of truth
